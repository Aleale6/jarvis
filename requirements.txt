# Core voice assistant dependencies (Windows 10)

# Text-to-speech (offline, uses Windows SAPI5 voices)
pyttsx3==2.90

# Speech recognition (speech-to-text)
SpeechRecognition==3.10.4

# Microphone access for SpeechRecognition.
# On Windows 10, modern pip ships prebuilt wheels, so this usually "just works":
#   pip install PyAudio
# If it fails, install via pipwin:
#   pip install pipwin && pipwin install pyaudio
PyAudio==0.2.14

# HTTP client used by every AI provider (OpenAI / Anthropic / Gemini / local).
# This is the only dependency the multi-provider router needs - each provider
# talks to its REST API directly over HTTP, so no per-vendor SDK is required.
requests==2.32.3
